# Crushr front end

## This project uses React + Vite

### To run:
First run the Crushr back end making sure that it is configured to allow for front end connections 
Then in crushr-frontend run: 
`npm run dev`
